namespace ODataUriHelper.Application
{
    /// <summary>
    /// A collection of supported OData v4 query clauses.
    /// </summary>
    internal enum ODataClause
    {
        Compute,
        Expand,
        Filter,
        OrderBy,
        Select,
        Skip,
        Top
    }
}
